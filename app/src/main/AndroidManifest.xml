<?xml version="1.0" encoding="utf-8"?>
<manifest package="org.proverbio.android.material"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <permission
        android:name="org.proverbio.android.INTERNAL"
        android:protectionLevel="signature"/>

    <application
        android:name="org.proverbio.android.context.ApplicationContext"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity
            android:name="org.proverbio.android.activity.AppMainActivity"
            android:label="@string/app_name"
            android:configChanges="orientation|screenSize"
            android:theme="@style/AppTheme">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name="org.proverbio.android.activity.ImageViewerActivity"
            android:parentActivityName="org.proverbio.android.activity.AppMainActivity"
            android:permission="org.proverbio.android.INTERNAL"
            android:configChanges="orientation|screenSize"
            android:theme="@style/AppTheme">

            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="org.proverbio.android.activity.AppMainActivity"/>
        </activity>

        <activity
            android:name="org.proverbio.android.fragment.location.LocationPickerActivity"
            android:parentActivityName="org.proverbio.android.activity.AppMainActivity"
            android:permission="org.proverbio.android.INTERNAL"
            android:configChanges="orientation|screenSize">

            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="org.proverbio.android.activity.AppMainActivity"/>
        </activity>

        <receiver android:name="org.proverbio.android.fragment.geofence.GeofenceRebootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_api_key" />
    </application>
</manifest>
